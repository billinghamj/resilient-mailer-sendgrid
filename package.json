{
	"name": "resilient-mailer-sendgrid",
	"version": "0.1.2",
	"description": "SendGrid provider implementation for resilient-mailer",

	"license": "MIT",
	"author": "James Billingham <james@jamesbillingham.com> (http://jamesbillingham.com)",

	"main": "lib/mailgun-provider.js",

	"repository": {
		"type": "git",
		"url": "https://github.com/billinghamj/resilient-mailer-sendgrid.git"
	},

	"scripts": {
		"test": "tape test/*.js",
		"test-travis": "istanbul cover test/*.js --report lcovonly"
	},

	"dependencies": {
		"form-data": "~0.1.3"
	},

	"devDependencies": {
		"tape": "~2.13.3",
		"istanbul": "~0.3.2"
	}
}
